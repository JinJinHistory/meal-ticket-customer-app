// commonStyles.js

import { StyleSheet } from 'react-native';

const theme = {
	primaryColor: '#535ce8',
	disabledColor: '#ddd',
};

const commonStyles = StyleSheet.create({
	pageLayout: {
		padding: 15,
	}
});

export { commonStyles, theme };
